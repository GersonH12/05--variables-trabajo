// 1. Calcular el área de un rectángulo: Pide al usuario la base y la altura de un rectángulo, y 
// muestra el área en la consola.

const base = parseFloat(prompt("Ingerese la base dedl rectangulo"));
const altura = parseFloat(prompt("Ingrese la altura del rectangulo"));

const area = base * altura;

console.log(`el area es:${area}`);